package net.iridiummc.iny.value;

/** The semantic kinds represented by the immutable INY value tree. */
public enum InyValueType {
    SECTION,
    LIST,
    STRING,
    INTEGER,
    DECIMAL,
    BOOLEAN,
    NULL,
    CALL
}
